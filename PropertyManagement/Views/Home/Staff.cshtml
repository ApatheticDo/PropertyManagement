<style>
    .banned-row {
    background-color: lightslategrey;
}
</style>


<div class="row">
    <div class="col-md-12">
        @* @if (Model.PageSize != null && Model.PageSize > 0)
        { *@
            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>UserName</th>
@*                         <th>Password</th> *@
                        <th>RoleName</th>
                        <th>CreatedBy</th>
                        <th>CreatedOn</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var s in ViewBag.StaffUser)
                    {
                    <tr class="@(s.IsAction ? "" : "banned-row")">
                            <td>@s.UserID</td>
                            <td>@s.UserName</td>
                            <td>@s.Role.RoleName</td>
                            <td>@s.CreatedBy</td>
                            <td>@s.CreatedOn</td>
                            <td>@(s.IsAction ? "Is Active" : "Is Banned")</td>
                            <td>
                            <a href="@Url.Action("UsersActive", "Home", new { s.UserID })" class="btn btn-primary" onclick="return confirm('Are you sure you want to ban/unban this user')">@(s.IsAction ? "Ban" : "Unban")</a>
                               @*<form asp-action="DeleteConfirmed" asp-route-id="@emp.Id" method="post" onsubmit="return confirm('Are you sure you want to delete this employee?')">
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form> *@
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        @* }
        else
        {
            <p>No employees found.</p>
        } *@
    </div>
